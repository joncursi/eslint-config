version: 2.0
jobs:
  build:
    docker:
      - image: circleci/node:11.0.0
    working_directory: ~/cheddur/eslint-config
    steps:
      - checkout
      - restore_cache:
          key: npm-cache-{{ .Branch }}-{{ checksum "package.json" }}
      - run: npm install
      - save_cache:
          key: npm-cache-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - node_modules
      - run: npm test
